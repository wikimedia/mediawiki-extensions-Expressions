{
  "require-dev": {
    "php-parallel-lint/php-parallel-lint": "*",
    "mediawiki/mediawiki-codesniffer": "*"
  },
  "scripts": {
    "test": [
      "parallel-lint . --exclude vendor",
      "phpcs -p -s"
    ],
    "fix": "phpcbf"
  }
}